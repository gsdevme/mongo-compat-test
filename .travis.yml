language: minimal

jobs:
    include:
        -   name: "mongo 3.6"
            env: MONGO_VERSION=mongo36
        -   name: "mongo 4.0"
            env: MONGO_VERSION=mongo40

services:
    - docker

script:
    - make run mongo=$(MONGO_VERSION)
